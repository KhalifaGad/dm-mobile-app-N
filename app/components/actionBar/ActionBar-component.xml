
<ActionBar title="AB" class="ActionBar-component" loaded="onLoad">
    <StackLayout class="AB-holder" orientation="horizontal" horizontalAlignment="left" >
        <Image id="ABLogo" class="ActionBar-logo" src="res://logo_white" />
        <Button id="sideMenuBtn" text="" class="side-drawer-btn" visibility="{{ hidden ? 'collapsed' : 'visible' }}" tap="openSideDrawer" />
    </StackLayout>   
</ActionBar>
