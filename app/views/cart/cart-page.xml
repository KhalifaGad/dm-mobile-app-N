<Page id="cart-page" actionBarHidden="{{ actionBarStatus.hidden }}" 
    xmlns="http://schemas.nativescript.org/tns.xsd" 
    xmlns:lv="nativescript-ui-listview" 
    xmlns:actionBar="components/actionBar" 
    xmlns:bottomNav="components/bottomNav" navigatingTo="onNavigatingTo" class="page">

    <Page.actionBar>
        <actionBar:ActionBar-component />
    </Page.actionBar>

    <AbsoluteLayout id="secondChild">
        <Label top="30" left="40" class="bold app-headers" text="MY CART" />
        <StackLayout top="100" left="40" orientation="horizontal" class="cart-headers-wrapper">
            <StackLayout orientation="vertical" horizontalAlignment="left" class="cart-info-wrapper">
                <StackLayout orientation="horizontal">
                    <Label text="Items: " textWrap="true" />
                    <StackLayout orientation="horizontal">
                        <Label text="{{ viewModel.items.length }}" textWrap="true" class="bold" />
                        <Label text=" item" textWrap="true" />
                    </StackLayout>
                </StackLayout>
                <StackLayout orientation="horizontal">
                    <Label text="Total: " textWrap="true" />
                    <StackLayout orientation="horizontal" >
                        <Label text="{{ viewModel.total }}" textWrap="true" class="bold" />
                        <Label text=" EGP" textWrap="true" />
                    </StackLayout>
                </StackLayout>
            </StackLayout>
            <Button text="checkout" tap="" horizontalAlignment="right" class="checkout-btn" tap="test" />
        </StackLayout>
        <StackLayout id="items-container" top="200" left="0" orientation="vertical" class="home-items-stklayout">
            <ActivityIndicator 
                horizontalAlignment="center"
                marginTop="139"
                visibility="{{viewModel.activityIndicatorVis}}"
                id="myIndicator"
                busy="{{ viewModel.notFetched }}"
                backgroundColor="transparent"
                color="#FF3838"
                width="100"
                height="100" />
            <ScrollView height="360" id="itemsScrollView" visibility="{{ viewModel.itemsViewVisibility }}" scrollBarIndicatorVisible="false">
                <StackLayout height="360" id="itemsStackLayout">
                    <ListView id="itemsListView" height="360" items="{{ viewModel.items }}" style="" itemTap="onItemTap">
                        <ListView.itemTemplate>
                            <StackLayout orientation="horizontal" class="list-item" tap="onTap" val="{{ $value }}">
                                <StackLayout orientation="horizontal" class="drug-info-wrapper">
                                    <StackLayout orientation="vertical" horizontalAlignment="right" verticalAlignment="middle" class="drug-name-price">
                                        <Label text="{{ $value.name }}" horizontalAlignment="left" class="drug-name" textWrap="true" />
                                        <Label text="{{'Price per pack ' + $value.price + 'EGP' }}" horizontalAlignment="left" textWrap="true" />
                                    </StackLayout>
                                    <StackLayout class="discount-box" orientation="vertical" horizontalAlignment="right">
                                        <Label text="{{ '-' + $value.discount + '%' }}" horizontalAlignment="center" />
                                    </StackLayout>
                                </StackLayout>
                                <Button text="" tap="toDrug" class="to-drug-btn" />
                            </StackLayout>
                        </ListView.itemTemplate>
                    </ListView>
                </StackLayout>
            </ScrollView>
            <StackLayout height="360" visibility="{{ viewModel.isEmptyViewVisibility }}" >
                <Label horizontalAlignment="center" text="Cart is empty" class="is-empty-lbl" textWrap="true"  />
            </StackLayout>
        </StackLayout>
        <bottomNav:BottomNav-component top="579" left="0" />
    </AbsoluteLayout>
</Page>