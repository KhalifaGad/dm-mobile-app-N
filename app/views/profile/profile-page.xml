<Page id="profile-page" actionBarHidden="{{ viewModel.isActionBarHidden }}"
    xmlns="http://schemas.nativescript.org/tns.xsd" 
    xmlns:lv="nativescript-ui-listview" 
    xmlns:actionBar="components/actionBar" 
    xmlns:bottomNav="components/bottomNav" navigatingTo="onNavigatingTo" class="page">
    <Page.actionBar>
        <actionBar:ActionBar-component />
    </Page.actionBar>
    <AbsoluteLayout id="secondChild">
        <Label top="30" left="24" class="bold app-headers" text="{{ viewModel.pharmacyName }}" />
        <StackLayout top="100" left="24" class="item-headers-wrapper">
            <FlexboxLayout flexDirection="{{viewModel.flexboxDirection}}" orientation="horizontal" class="first-layer">
                <StackLayout orientation="vertical" class="item-info-wrapper">
                    <FlexboxLayout flexDirection="{{viewModel.flexboxDirection}}"  orientation="horizontal">
                        <Label text="{{viewModel.ordersString}}" textWrap="true" />
                        <Label text="{{ viewModel.ordersCount }}" textWrap="true" class="bold" />
                    </FlexboxLayout>
                    <FlexboxLayout flexDirection="{{viewModel.flexboxDirection}}" >
                        <Label text="{{viewModel.totalString}}" textWrap="true" />
                        <Label text="{{viewModel.ordersTotal}}" textWrap="true" class="bold" />
                        <Label text="{{viewModel.currencyString}}" textWrap="true" class="bold" />
                    </FlexboxLayout>
                    <FlexboxLayout flexDirection="{{viewModel.flexboxDirection}}" >
                        <Label text="{{viewModel.walletString}}" textWrap="true" />
                        <Label text="{{viewModel.wallet}}" textWrap="true" class="bold" />
                    </FlexboxLayout>
                </StackLayout>
                <Image src="res://edit"  width="46vw" height="46vh" tap="toSettings"/>
            </FlexboxLayout>
        </StackLayout>
        <StackLayout id="items-container" top="230" left="0" orientation="vertical" class="home-items-stklayout">
            <StackLayout visibility="{{ viewModel.adViewVisbility }}" class="items-stacklayout" > 
                    <Label text="advertisement area" horizontalAlignment="center" verticalAlignment="middle" />
            </StackLayout>
            <!-- <ActivityIndicator 
                horizontalAlignment="center"
                marginTop="139"
                visibility="{{viewModel.activityIndicatorVis}}"
                id="myIndicator"
                busy="{{ viewModel.notFetched }}"
                backgroundColor="transparent"
                color="#FF3838"
                width="100"
                height="100" />
            <ScrollView class="home-items-scrollView" id="itemsScrollView" scrollBarIndicatorVisible="false">
                <StackLayout class="items-stacklayout" visibility="{{ viewModel.itemsViewVisibility }}" id="itemsStackLayout">
                    <ListView id="itemsListView" class="items-listView" items="{{ viewModel.items }}" style="" itemTap="onItemTap">
                        <ListView.itemTemplate>
                            <StackLayout orientation="horizontal" class="list-item" val="{{ $value }}">
                                <StackLayout orientation="horizontal" class="drug-info-wrapper">
                                    <StackLayout orientation="vertical" horizontalAlignment="right" verticalAlignment="middle" class="drug-name-price">
                                        <Label text="{{ $value.name }}" horizontalAlignment="left" class="drug-name" textWrap="true" />
                                        <Label text="{{'Price per pack ' + $value.price + 'EGP' }}" horizontalAlignment="left" textWrap="true" />
                                    </StackLayout>
                                    <StackLayout class="discount-box" orientation="vertical" horizontalAlignment="right">
                                        <Label text="{{ '-' + $value.discount + '%' }}" horizontalAlignment="center" />
                                    </StackLayout>
                                </StackLayout>
                                <Button text="" tap="toDrug" class="to-drug-btn" />
                            </StackLayout>
                        </ListView.itemTemplate>
                    </ListView>
                </StackLayout>
            </ScrollView> -->
        </StackLayout>
        <!-- <bottomNav:BottomNav-component top="579" left="0" /> -->
    </AbsoluteLayout>
</Page>