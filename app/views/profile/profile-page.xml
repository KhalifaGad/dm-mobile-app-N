<Page id="profile-page" actionBarHidden="{{ actionBarStatus.hidden }}" 
    xmlns="http://schemas.nativescript.org/tns.xsd" 
    xmlns:lv="nativescript-ui-listview" 
    xmlns:actionBar="components/actionBar" 
    xmlns:bottomNav="components/bottomNav" navigatingTo="onNavigatingTo" class="page">
    <Page.actionBar>
        <actionBar:ActionBar-component />
    </Page.actionBar>
    <AbsoluteLayout id="secondChild">
        <Label top="30" left="24" class="bold app-headers" text="Dr.Ahmed Pharmacy" />
        <StackLayout top="100" left="24" class="item-headers-wrapper">
            <StackLayout orientation="horizontal" class="first-layer">
                <StackLayout orientation="vertical" horizontalAlignment="left" class="item-info-wrapper">
                    <StackLayout orientation="horizontal">
                        <Label text="City: " textWrap="true" />
                        <Label text="Alexandria" textWrap="true" class="bold" />
                    </StackLayout>
                    <StackLayout orientation="horizontal">
                        <Label text="Area: " textWrap="true" />
                        <Label text="Smouha" textWrap="true" class="bold" />
                    </StackLayout>
                </StackLayout>
                <Image horizontalAlignment="right" src="res://edit" width="46vw" height="46vh" tap="toSettings"/>
            </StackLayout>
        </StackLayout>
        <StackLayout id="items-container" top="200" left="0" orientation="vertical" class="home-items-stklayout">
            <ScrollView height="360" id="itemsScrollView" scrollBarIndicatorVisible="false">
                <StackLayout height="360" id="itemsStackLayout" >
                    <!-- loaded="onLoaded" itemLoading="onItemLoading" -->
                    <ListView id="itemsListView" height="360" items="{{ viewModel.items }}" style="" itemTap="onItemTap">
                        <ListView.itemTemplate>
                            <StackLayout orientation="horizontal" class="list-item" tap="onTap" val="{{ $value.item }}">
                            <StackLayout orientation="horizontal" class="drug-info-wrapper">
                                <StackLayout orientation="vertical" horizontalAlignment="right" verticalAlignment="middle" class="drug-name-price">
                                    <Label text="{{ $value.item }}" horizontalAlignment="left" class="drug-name" textWrap="true" />
                                    <Label text="{{'Price per pack ' + $value.price + 'EGP' }}" horizontalAlignment="left" textWrap="true" />
                                </StackLayout>
                                <StackLayout class="discount-box" orientation="vertical" horizontalAlignment="right">
                                    <Label text="{{ '-' + $value.discount + '%' }}" horizontalAlignment="center" />
                                </StackLayout>
                            </StackLayout>
                            <Image src="res://fa_angle_right" horizontalAlignment="right" style="horizontal-align:center;" verticalAlignment="middle" height="20vh" width="20vw" />
                        </StackLayout>
                        </ListView.itemTemplate>
                    </ListView>
                </StackLayout>
                <!-- <Repeater items="{{ viewModel.items }}">
                    <Repeater.itemTemplate>
                        <StackLayout orientation="horizontal" class="list-item" tap="onTap" val="{{ $value.item }}">
                            <StackLayout orientation="horizontal" class="drug-info-wrapper">
                                <StackLayout orientation="vertical" horizontalAlignment="right" verticalAlignment="middle" class="drug-name-price">
                                    <Label text="{{ $value.item }}" horizontalAlignment="left" class="drug-name" textWrap="true" />
                                    <Label text="{{'Price per pack ' + $value.price + 'EGP' }}" horizontalAlignment="left" textWrap="true" />
                                </StackLayout>
                                <StackLayout class="discount-box" orientation="vertical" horizontalAlignment="right">
                                    <Label text="{{ '-' + $value.discount + '%' }}" horizontalAlignment="center" />
                                </StackLayout>
                            </StackLayout>
                            <Image src="res://fa_angle_right" horizontalAlignment="right" style="horizontal-align:center;" verticalAlignment="middle" height="20vh" width="20vw" />
                        </StackLayout>
                    </Repeater.itemTemplate>
                </Repeater> -->
            </ScrollView>
        </StackLayout>
        <bottomNav:BottomNav-component top="579" left="0" />
    </AbsoluteLayout>
</Page>